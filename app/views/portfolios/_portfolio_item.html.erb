<div class="col-md-4">
  <div class="card" data-id="<%= portfolio_item.id %>">
    <a href="<%= portfolio_show_path(portfolio_item) %>">
      <%= image_tag portfolio_img(portfolio_item.thumb_image, 'thumb'), width: '100%' %></p>
    </a>
    <p class="card-text"><%= link_to portfolio_item.title, portfolio_show_path(portfolio_item) %></p>
    <p class="card-text"><%= portfolio_item.subtitle %></p>
    </p>
  </div>
</div>
